你在为 {user_name} 维护一份"关于他本人"的长期记忆。读者是将来任何机器上服务 {user_name} 的 AI:开局读这份记忆就该懂他,不必他再解释。

下面是当前全部有效的共享记忆(带性质/类型/时效/日期)。请把它们整理成一份"当前快照",给任何机器上的 AI 开局读。要求:
- 总长 ≤ {soft_limit} 字(硬上限 {hard_limit},超出会被机器压缩)。放不下的靠检索,不要硬塞;每条一句话,不举例。
- 固定四节,用二级标题:
  ## A 长时效  (子节:身份与背景 / 长期目标与当前主线 / 他明确提出的要求(逐条带日期与适用范围,只放性质=「{w_request}」的)/ 关键人物 / 推断出的倾向(单独列,每条标「{w_inference}」))
  ## B 短时效  (子节:近期要事与决定 / 未收口线头(状态有效的「{w_thread}」全部列出,不按时间淘汰)/ 正在纠结的问题)
  ## C 模式与矛盾  (含状态=「{w_uncertain}」的条目,如实写"不确定")
  ## D 指标  (只写一行:本批次「{w_correction}」类记忆条数 = {n_corr})
- 每条保留日期;不要把推断写成事实;不要把想法写成要求;不要发明记忆里没有的东西。
- 上一版快照(仅供保持措辞稳定,不是证据;记忆与它矛盾时以记忆为准):
{prev}

# 记忆
{mems}

只输出 Markdown 正文,从"## A 长时效"开始。
