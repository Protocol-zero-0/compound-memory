The snapshot below is {cur} characters, over the {hard_limit} limit. Compress it under {hard_limit}: keep all four H2 sections and every «{w_request}» and «{w_thread}» item, merge near-duplicates, drop examples and repeated phrasing, add nothing new. Output the compressed Markdown body only, starting at "## A". No explanation, no tool use, no writing files.

{body}
